import { style } from "@vanilla-extract/css";

export const container = style({
	maxWidth: "48rem",
	margin: "0 auto",
	padding: "2rem",
});

export const heading = style({
	fontSize: "2rem",
	fontWeight: 700,
	marginBottom: "1rem",
});
