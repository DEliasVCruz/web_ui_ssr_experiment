import { style } from "@vanilla-extract/css";

export const container = style({
	margin: "0 auto",
	padding: "2rem",
	maxInlineSize: "48rem",
});

export const heading = style({
	marginBlockEnd: "1rem",
	fontSize: "2rem",
	fontWeight: 700,
});
